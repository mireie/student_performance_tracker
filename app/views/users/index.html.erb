<%= content_for(:title, "Users") %>
<h1>Users</h1>
<hr>
<div class="row">
  <div class="col">
    <% if @users.any? %>
      <%= render :partial => "user_grid", :locals => {:users => @users} %>
    <% else%>
      <p>There are no users yet.</p>
    <% end %>
  </div>
</div>
