<p id="notice"><%= notice %></p>

<p><%= link_to 'New Teacher', new_teacher_path, class: "btn btn-primary" %></p>

<h1>Teachers</h1>
<hr>

<div class="row">
  <div class="col">
    <% if @teachers.any? %>
   
      <%= render :partial => "teacher_grid", :locals => {:teachers => @teachers} %>
    
    <% else%>
      <p>There are no teachers yet.</p>
    <% end %>
  </div>
</div>