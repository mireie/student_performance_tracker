<%= content_for(:title, "#{@student.last_name}, #{@student.first_name} | Add DIBELS score") %>
<h1>Add a new DIBELS score for <%=@student.first_name%> <%= @student.last_name %>:</h1>

<p><%= link_to 'Return to Student Page', student_path(@student) %></p>

<%= render :partial => "/progresses/edit_progress", :locals => {:student => @student}  %>
